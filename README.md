# PHP OOP Exam – Product List (Abstract Class & Interface)

این ریپازیتوری مربوط به یک **امتحان عملی کوتاه** برای درس **Object-Oriented Programming in PHP** است.

در این پروژه باید با استفاده از مفاهیم زیر کار کنید:

* `class` و `object`
* `property` و `method`
* `constructor` (`__construct`)
* access modifiers (`public` / `protected` / `private`)
* **`abstract class`**
* **`interface`**
* **inheritance** (`extends`)
* استفادهٔ ساده از **PDO** برای خواندن داده‌ها از دیتابیس

هدف اصلی این پروژه این است که بتوانید:

* یک `abstract class` طراحی کنید.
* یک `interface` تعریف کنید.
* دو کلاس فرزند (`subclass`) که از `abstract class` ارث‌بری می‌کنند و `interface` را پیاده‌سازی می‌کنند، بسازید.
* با استفاده از این کلاس‌ها، داده‌های دیتابیس را در صفحهٔ وب نمایش دهید.

در این README فقط چیزهایی نوشته شده که برای شما (دانشجو) مهم است و به حل بهتر تمرین کمک می‌کند.

---

## 1. ساختار پروژه

ساختار پوشه‌ها و فایل‌ها:

```text
php-oop-exam-products/
  README.md               ← همین فایل
  INSTRUCTOR_NOTES.md     ← توضیحات مخصوص مدرس (برای شما مهم نیست)
  .gitignore
  db/
    db.php                ← اتصال به دیتابیس (PDO)
    init.sql              ← ساخت جدول و داده اولیه
  config/
    student.php           ← اطلاعات شما (دانشجو)
  src/
    Product.php           ← abstract class
    Sellable.php          ← interface
    Book.php              ← کلاس Book
    Notebook.php          ← کلاس Notebook
  public/
    index.php             ← صفحهٔ اصلی سایت
    assets/
      style.css           ← استایل آماده
```

شما قرار است در چند فایل خاص (`student.php`, `Product.php`, `Book.php`, `Notebook.php`, `index.php`) قسمت‌های `TODO` را کامل کنید.
کد HTML و CSS آماده است و لازم نیست استایل را عوض کنید (مگر برای اصلاح کوچک در صورت نیاز).

---

## 2. قوانین و شرایط امتحان

1. زمان امتحان تقریباً **۳۰ تا ۴۰ دقیقه** است (طبق اعلام مدرس در کلاس).
2. شما می‌توانید از:

   * جزوه‌های خودتان
   * داکیومنت رسمی (مثلاً PHP manual، W3Schools، MDN و …
   * جست‌وجوی ساده برای syntax
     استفاده کنید.
3. شما **مجاز به استفاده از ابزارهای هوش مصنوعی** (مثل ChatGPT، Copilot، Codeium و …) برای نوشتن یا تکمیل کد این پروژه **نیستید**.
4. شما **مجاز نیستید** کد خود را برای دیگران بفرستید، از روی کد دیگران کپی کنید، یا از ریپازیتوری هم‌کلاسی خودتان استفاده کنید.
5. نام فایل‌ها و ساختار پروژه را تغییر ندهید. فقط قسمت‌های `TODO` را کامل کنید.
6. تلاش کنید کد را به زبان خودتان و با فهم خودتان بنویسید. اگر بعداً مدرس از روی کد از شما سؤال شفاهی بپرسد، باید بتوانید کار کد خودتان را توضیح دهید.

---

## 3. مراحل راه‌اندازی (Setup)

1. این ریپازیتوری را در GitHub **Fork** کنید (روی اکانت خودتان).
2. ریپازیتوری Fork شده را در سیستم خودتان `clone` کنید.
3. در MySQL یک دیتابیس جدید بسازید، مثلاً با نام `oop_exam`.
4. فایل `db/init.sql` را روی دیتابیس اجرا کنید تا:

   * جدول `products` ساخته شود.
   * چند ردیف نمونه (محصولات) وارد شود.
5. در فایل `db/db.php`، تنظیمات اتصال (`$dsn`, `$user`, `$pass`) را با توجه به سیستم خودتان تنظیم کنید:

   * `host`
   * `dbname`
   * `username`
   * `password`
6. پروژه را روی وب‌سرور PHP (مثلاً XAMPP، Laragon، یا PHP Built-in Server) اجرا کنید و آدرس شبیه زیر را در مرورگر باز کنید:

   * `http://localhost/php-oop-exam-products/public/index.php`

در ابتدای کار (قبل از انجام TODOها) صفحه تقریباً خالی است و فقط یک پیام ساده می‌بینید.
بعد از تکمیل همهٔ بخش‌ها، کارت‌های محصولات با ظاهر کامل نمایش داده می‌شود.

---

## 4. اطلاعات دانشجو (Task 0)

**فایل:** `config/student.php`

در این فایل سه `constant` وجود دارد که باید آن‌ها را پر کنید:

* `STUDENT_ID` → شماره دانشجویی شما (مثال: `"40123456"`)
* `STUDENT_NAME` → نام و نام خانوادگی کامل (مثال: `"Sara Ahmadi"`)
* `STUDENT_SECRET_PHRASE` → یک جملهٔ کوتاه به زبان فارسی که خودتان انتخاب می‌کنید و برای شما خاص است.

مثال برای `STUDENT_SECRET_PHRASE`:

* `"من همیشه قبل از کدنویسی چای می‌خورم."`
* `"برنامه‌نویسی برای من مثل حل معماست."`

این جمله در فوتر سایت نمایش داده می‌شود تا هر پروژه **شخصی و یکتا** باشد.

---

## 5. سناریوی کلی پروژه

یک فروشگاه بسیار ساده داریم که دو نوع محصول دارد:

* `Book` (کتاب)
* `Notebook` (دفتر)

اطلاعات محصولات در دیتابیس داخل جدول `products` ذخیره شده است.
فایل `db/db.php` با استفاده از PDO داده‌ها را می‌خواند و در متغیر `$rows` قرار می‌دهد.

شما باید:

1. یک `abstract class` به نام `Product` بسازید که اطلاعات مشترک محصول را نگه‌داری کند.
2. یک `interface` به نام `Sellable` بسازید که متد `getFinalPrice()` داشته باشد.
3. دو کلاس `Book` و `Notebook` بسازید که:

   * از `Product` ارث‌بری کنند (`extends Product`)
   * `Sellable` را پیاده‌سازی کنند (`implements Sellable`)
4. در `public/index.php`:

   * روی آرایهٔ `$rows` حلقه بزنید.
   * بر اساس ستون `type`، شیء مناسب (`Book` یا `Notebook`) بسازید.
   * این شیءها را در آرایهٔ `$products` قرار دهید.
   * در بخش HTML از متدهای این شیءها برای نمایش اطلاعات استفاده کنید.

---

## 6. شرح Tasks (کاری که باید انجام دهید)

### Task 1 – پیاده‌سازی `abstract class Product`

**فایل:** `src/Product.php`

کلاس `Product` باید:

1. یک کلاس انتزاعی باشد:

   ```php
   abstract class Product
   ```

2. سه `property` زیر را به صورت `protected` داشته باشد:

   ```php
   protected int $id;
   protected string $title;
   protected int $price;
   ```

3. یک `constructor` با امضای زیر پیاده‌سازی کنید:

   ```php
   public function __construct(int $id, string $title, int $price)
   ```

   که مقدارهای ورودی را در `properties` ذخیره کند.

4. متدهای زیر را پیاده‌سازی کنید:

   ```php
   public function getTitle(): string
   public function getPrice(): int
   ```

5. متد زیر باید به صورت `abstract` باقی بماند (فقط امضا، بدون بدنه):

   ```php
   abstract public function getTypeLabel(): string;
   ```

**نکته:** در این پروژه نیازی به `destructor` نداریم و نباید از آن استفاده کنید.

---

### Task 2 – پیاده‌سازی `interface Sellable`

**فایل:** `src/Sellable.php`

در این فایل یک `interface` به نام `Sellable` داریم.

این interface باید متد زیر را داشته باشد:

```php
interface Sellable
{
    public function getFinalPrice(): int;
}
```

این متد در کلاس‌های `Book` و `Notebook` پیاده‌سازی می‌شود.

---

### Task 3 – کلاس `Book`

**فایل:** `src/Book.php`

کلاس `Book` نمایندهٔ یک محصول از نوع کتاب است و باید:

1. به این صورت تعریف شود:

   ```php
   class Book extends Product implements Sellable
   ```

2. یک `constructor` با امضای زیر داشته باشد:

   ```php
   public function __construct(int $id, string $title, int $price)
   ```

   این constructor باید `id`, `title`, `price` را در `properties` (که از `Product` به ارث رسیده‌اند) ذخیره کند.

   برای این کار می‌توانید مستقیماً به `$this->id`, `$this->title`, `$this->price` مقدار بدهید.
   در این تمرین نیازی نیست از `parent::__construct()` استفاده کنید.

3. متد `getTypeLabel()` را پیاده‌سازی کنید تا دقیقا رشتهٔ `"Book"` را برگرداند.

4. متد `getFinalPrice()` را طوری پیاده‌سازی کنید که:

   * قیمت محصول را با **۱۰٪ تخفیف** برگرداند.
   * مثال: اگر `price = 100000` باشد، خروجی `90000` است.
   * می‌توانید از این فرمول استفاده کنید:

     ```php
     return (int) round($this->price * 0.9);
     ```

---

### Task 4 – کلاس `Notebook`

**فایل:** `src/Notebook.php`

کلاس `Notebook` نمایندهٔ دفتر است و باید:

1. به این صورت تعریف شود:

   ```php
   class Notebook extends Product implements Sellable
   ```

2. یک `constructor` مانند کلاس `Book` داشته باشد:

   ```php
   public function __construct(int $id, string $title, int $price)
   ```

   و `id`, `title`, `price` را در `properties` ذخیره کند.

3. متد `getTypeLabel()` را پیاده‌سازی کنید تا دقیقا رشتهٔ `"Notebook"` را برگرداند.

4. متد `getFinalPrice()` را طوری پیاده‌سازی کنید که:

   * همان `price` اصلی را **بدون تخفیف** برگرداند.

---

### Task 5 – ساخت اشیاء در `public/index.php`

**فایل:** `public/index.php`

در این فایل:

* با `require` کردن `db/db.php` یک اتصال PDO ساخته شده است.
* کوئری `SELECT` اجرا شده است.
* نتایج در آرایهٔ `$rows` قرار گرفته است.
* آرایهٔ `$products` خالی تعریف شده است.

شما باید:

1. روی `$rows` یک حلقهٔ `foreach` بنویسید.
2. برای هر `$row`:

   * اگر `$row['type'] === 'book'` بود، یک شیء `Book` بسازید.
   * اگر `$row['type'] === 'notebook'` بود، یک شیء `Notebook` بسازید.
3. شیء ساخته شده را به آرایهٔ `$products` اضافه کنید.

در نهایت، آرایهٔ `$products` باید شامل شیءهای `Book` و `Notebook` باشد.

---

### Task 6 – نمایش محصولات در HTML

**فایل:** `public/index.php`

در بخش HTML، اگر آرایهٔ `$products` خالی باشد، یک پیام ساده نمایش داده می‌شود.
اگر `products` خالی نباشد، برای هر محصول یک کارت (`card`) نمایش داده می‌شود.

در بخش‌هایی که به صورت `TODO(Task 6)` مشخص شده، باید:

* عنوان محصول را با `getTitle()` چاپ کنید.
* نوع محصول را با `getTypeLabel()` چاپ کنید.
* قیمت اصلی را با `getPrice()` چاپ کنید.
* قیمت نهایی را با `getFinalPrice()` چاپ کنید.

بعد از تکمیل Task 5 و Task 6، به‌جای پیام خالی، کارت‌های محصولات را به‌صورت شبکه‌ای (grid) روی صفحه خواهید دید.

---

## 7. Commit کردن و تحویل

1. بعد از هر چند Task که انجام دادید، یک `commit` بزنید.
   (مثلاً بعد از Task 1 و 2، یک commit؛ بعد از Task 3 و 4، یک commit؛ و …)
2. پیام commit را طوری بنویسید که مشخص باشد چه کار کرده‌اید، مثلاً:

   * `Finish Product abstract class`
   * `Implement Book and Notebook classes`
   * `Build products list in index.php`
3. در پایان، کد خود را `push` کنید تا روی GitHub شما قرار بگیرد.
4. لینک ریپازیتوری را طبق دستور مدرس تحویل دهید.

---

موفق باشید 🌱

اگر هر بخش از صورت سؤال برای شما مبهم بود، حتماً قبل از شروع کدنویسی از مدرس سؤال کنید.
# exam1140825
